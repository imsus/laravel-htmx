## What does this change?

## Why is it needed?

## How was it verified?

- [ ] `composer test` passes (analyse + lint + type-coverage + Pest)

## Checklist

- [ ] Follows the house patterns in `README.md` (single-view `fragmentIf`, `422` error partial, header helpers)
- [ ] Docs updated (`README.md` / `CHANGELOG.md` / skill) if user-facing behavior changed
- [ ] No package routes, migrations, or translations added
